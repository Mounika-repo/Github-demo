name: dev
on:
  push:
     branches: [dev]
     
jobs:
  Offer_letter:   
    runs-on: windows-latest
    steps:
      - name: offer letter
        run: echo "I accepted offer letter,started onboarding process"
