name: workflow
on: [push]

jobs:
  build:   
    name: Hello world
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: run powershell script
      shell: pwsh
      run: |
        .\build.ps1
